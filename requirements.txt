# Core dependencies (Phase 1 — heuristic pipeline)
pdfplumber>=0.11.0,<1.0
python-docx>=1.1.0,<2.0
pydantic>=2.5.0,<3.0
PyYAML>=6.0,<7.0
structlog>=24.1.0,<25.0

# Phase 2 — multilingual NER
gliner>=0.2.0,<1.0
torch>=2.0.0,<3.0           # GLiNER dependency

# Phase 3 — local LLM verification
requests>=2.31.0,<3.0

# Testing
pytest>=8.0.0,<9.0
pytest-cov>=4.1.0,<5.0

# Dev tooling (optional but recommended)
ruff>=0.4.0,<1.0
mypy>=1.10.0,<2.0
